# BMM Module Configuration
# Generated by BMAD installer
# Version: 6.0.0-alpha.23
# Date: 2026-01-12T21:39:07.374Z

project_name: Bienvenue
user_skill_level: intermediate
planning_artifacts: "{project-root}/_bmad-output/planning-artifacts"
implementation_artifacts: "{project-root}/_bmad-output/implementation-artifacts"
project_knowledge: "{project-root}/docs"
tea_use_mcp_enhancements: false
tea_use_playwright_utils: false

# Core Configuration Values
user_name: Gabriel
communication_language: Français
document_output_language: Français
output_folder: "{project-root}/_bmad-output"

# ClickUp Integration
# ===================
# ClickUp COMPLÈTE (ne remplace pas) les autres sources de documentation.
# Hiérarchie des sources :
#   1. Architecture, PRD, Epics → Spécifications techniques (toujours consulter)
#   2. project-context.md → Standards de code (toujours suivre)
#   3. Story file (.md) → Acceptance criteria (autorité sur l'implémentation)
#   4. ClickUp comments → Contexte additionnel (décisions d'équipe, clarifications)
clickup:
  enabled: true
  workspace: "Bienvenue"
  space_id: "90151323878"
  folder: "Développement"
  list: "MVP"
  list_id: "901507766265"

  # Status mapping (ClickUp status → sprint-status.yaml status)
  status_mapping:
    "a faire": "backlog"
    "en cours": "in-progress"
    "review": "review"
    "termine": "done"

  # Agent workflow rules
  agent_rules:
    # RÈGLE FONDAMENTALE
    - "ClickUp fournit le CONTEXTE ADDITIONNEL sur la tâche (ne remplace pas architecture/PRD/specs)"
    - "Consulter BMAD docs (architecture, PRD, story) + ClickUp comments ensemble"

    # AVANT de commencer
    - "ALWAYS consult BMAD documents first (architecture, PRD, epics, story file)"
    - "THEN search ClickUp for the story and read comments for additional context"
    - "ClickUp comments can clarify/prioritize but don't override specs"

    # PENDANT le travail
    - "ALWAYS update ClickUp status when starting a story (→ en cours)"
    - "ALWAYS comment on ClickUp when making important decisions"
    - "ADD implementation notes as comments on the ClickUp task"

    # APRÈS le travail
    - "ALWAYS update ClickUp status when completing a story (→ review/termine)"
    - "ADD completion summary with files changed as final comment"
